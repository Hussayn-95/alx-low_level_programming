#include "main.h"
/**
 * _strcat - combine two strings
 * @s: input
 * return: always 0
 */
char *_strcat(char *dest, char *src)
{
	int i;
	int j = strlen[s];

	for (i = 0; s1[i] != '\0'; i++)
	{
		s[i +  j] = s1[i];
	}
	s[i + j} = '\0';
	return (0);

}
