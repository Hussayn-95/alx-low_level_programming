#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * main -  main function
 * @argv: prints the vector of arrays
 * @argc: refers to the arguments count
 * Return: 0 success
 */
int main(int argc, char *argv[])
{
	(void) argc;

	print("argv = %s\n", argv[0]);

	return (0);
}
